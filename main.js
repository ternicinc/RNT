const path = require("path");
const fs = require("fs");

// Custom Imports
// const ipManager = require("ipManager.js");

function main() {
    try {
        console.log('Hello, World.');
    }
    catch {
        error("ERROR")
    }
}

main();